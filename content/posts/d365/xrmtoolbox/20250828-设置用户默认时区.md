---
title: "D365 – 设置用户默认时区"
date: 2025-08-28
description: "介绍如何在 XrmToolBox 中设置用户的默认时区。通过使用 User Settings Utility，您可以轻松加载用户信息，选择特定用户和时区，并更新用户设置。此外，文章还提供了一段 SQL 查询，帮助您查看当前用户使用的时区信息。无论您是管理员还是用户，这些步骤都将帮助您更好地管理时区设置，以确保系统的时间显示准确。"
draft: false
slug: "set-default-time-zone-for-user"

# 系列
series: ["D365 Xrmtoolbox"]

# 分类和标签
categories: ["Dynamics 365","XrmToolBox"]
tags: ["Dynamics 365", "XrmToolBox"]

showDonate: true # 打赏
showDocInfo: true # 文档信息
UpdateDate: 2025-11-01
---

## 行内代码

这是一个行内代码示例：`const x = 42;` 和 `npm install` 以及 `git commit -m "更新"`。

## Code

```html {lineNos=true hl_lines=[3,6,8]}
{{ $lang := .Site.Language.Lang }}
<div class="doc-info-container">
  <div class="doc-info-meta-info">
    {{ if eq $lang "zh" }}
    <h4>文档信息</h4>
    <p>版权声明 : 自由转载-非商用-非衍生-保持署名 (<a href="{{- .Param "licenseLink" -}}" target="_blank">{{- .Param "licenseName" -}})</a></p>
    <p>发表日期 : {{ .Date | time.Format "2006-01-02" }}</p>
    {{ with .Params.UpdateDate }}
    <p>更新日期 : {{ . | time.Format "2006-01-02" }}</p>
    {{ end }}
    {{ else }}
    <h4>Document Information</h4>
    <p>License : <a href="{{- .Param "licenseLink" -}}" target="_blank">{{- .Param "licenseName" -}}</a></p>
    <p>Published Date : {{ .Date | time.Format "2006-01-02" }}</p>
    {{ with .Params.UpdateDate }}
    <p>Last Updated Date : {{ . | time.Format "2006-01-02" }}</p>
    {{ end }}
    {{ end }}
  </div>
</div>
```

## Title 1

设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区

## Title 2

设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区设置用户默认时区
